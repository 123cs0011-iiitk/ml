# Stock Prediction System Environment Variables
# Copy this file to .env and fill in your actual values

# Flask Configuration
PORT=5000
CACHE_DURATION=60
MIN_REQUEST_DELAY=2.0

# Finnhub API (for US stocks)
FINNHUB_API_KEY=your_finnhub_api_key_here

# Upstox API Configuration (for Indian stocks)
# Get these from: https://upstox.com/developer/
UPSTOX_API_KEY=your_api_key_here

# Upstox OAuth2 Credentials
# These will be generated by running: python backend/scripts/setup_upstox_oauth.py
UPSTOX_CLIENT_ID=your_client_id_here
UPSTOX_CLIENT_SECRET=your_client_secret_here
UPSTOX_REDIRECT_URI=http://localhost:8080

# Generated tokens (automatically filled by OAuth setup script)
UPSTOX_ACCESS_TOKEN=will_be_generated_by_oauth
UPSTOX_REFRESH_TOKEN=will_be_generated_by_oauth
UPSTOX_TOKEN_EXPIRY=will_be_generated_by_oauth

# Setup Instructions:
# 1. Get your Client ID and Client Secret from Upstox Developer Portal
# 2. Update UPSTOX_CLIENT_ID and UPSTOX_CLIENT_SECRET above
# 3. Run: python backend/scripts/setup_upstox_oauth.py
# 4. Follow the browser prompts to authorize your app
# 5. The script will automatically fill in all token values
